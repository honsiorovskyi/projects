# vim:ft=bash

p() {
  local target
  target=$(pgo --print "$@")
  if [[ -n "$target" ]]; then
    cd "$target" || return 1
  fi
}

pn() {
  local target
  target=$(pnew --print "$@")
  if [[ -n "$target" ]]; then
    cd "$target" || return 1
  fi
}

alias pa='padd'
